{
  "name": "soma-strava-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Garmin→Strava facterino bridge (TypeScript, GitHub Actions + Playwright). Port of sync/src/strava_bridge*.",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "bridge": "node dist/main.js"
  },
  "dependencies": {
    "oauth-1.0a": "^2.2.6",
    "pg": "^8.11.0",
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
